<div class="error main__section">
  {% if error.video %}
    <div class="error__video">
      <video class="video" src="{{ error.video.path }}" preload="metadata" autoplay="autoplay" loop muted playsinline></video>
    </div>
  {% endif %}
  <div class="container">
    <div class="error__inner">
      {% if error.type %}
        <div class="error__type">
          {{ error.type }}
        </div>
      {% endif %}

      {% if error.title %}
        {% view "@title" with {
          title: error.title|default({})|merge({
            class: "js-title error__title"
          })
        } %}
      {% endif %}

      {% if error.text %}
        <p class="error__text">
          {{ error.text }}
        </p>
      {% endif %}
    </div>
  </div>
</div>